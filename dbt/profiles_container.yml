uk_flight_punctuality:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: '# change to your GCP project ID'
      dataset: flight_data
      threads: 1
      timeout_seconds: 300
      location: europe-west2
      priority: interactive
      retries: 1
